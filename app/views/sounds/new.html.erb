<h1>投稿画面</h1>
<%= form_for(@sound) do |f| %>
	<label>タイトル</label>
	<%= f.text_field :title %>
	<label>url</label>
	<%= f.text_field :url %>
	<label>ID</label>
	<%= f.number_field :user_id %>
	<%= f.submit "投稿" %>
<%end%>