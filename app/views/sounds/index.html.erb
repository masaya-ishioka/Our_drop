<div class="row">
	<div class="col-md-12">
		<h1 class="center">投稿一覧</h1>
	</div>
	<div class="col-md-4"></div>
	<div class="col-md-4">
		<% @sounds.each do |sound| %>
			<label>曲名</label>
			<%= sound.title %>
			<%= link_to "詳細ページ", sound_path(sound.id)%>
		<%end%>
	</div>
	<div class="col-md-4"></div>
</div>